console.log("Hello welcome to Node JS")

//console.log(alert("hello"))  //DOM elements are removed as node js is for server side

//npm init will create package.json file which is the congiguration file that contains dependencies entry point

/*
In package.json file
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "hello.js"
  },
we have added the start property, this means we can now run this files using npm start which will frst execute hello.js
*/